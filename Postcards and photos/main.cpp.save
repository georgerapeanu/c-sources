#include <iostream>
#include <cstring>
using namespace std;
char C[200],fost;
int i,nr,N,rez;
int main()
{
    cin>>N;
    C[0]='.';
    cin.getline(C+1,200);
    cin.getline(C+1,200);
    N=strlen(C);
    for(i=1;i<N;i++)
    {
        if(nr==5)
        {
            rez++;
            nr=1;
        }
        else if(C[i]!=fost&&fost!='\0')
            n
    }
    return 0;
}
