#include <cstdio>
using namespace std;
FILE *f=fopen("culmi.in","r");
FILE *g=fopen("culmi.out","w");
int V[10050];
int i,j,nr,rez;
bool rezolvat;
int main()
{
    fscanf(f,"%d%d",&N);
    for(i=1;i<=N;i++)
    {
        fscanf(f,"%d",&nr);
        rezolvat=0;
        for(j=1;j<=rez&&!rezolvat;i++)
        {
            if(V[j]<nr)
            {
                V[j]=nr;
                rezolvat=1;
            }
        }
        if(!rezolvat)
        {
            V[rez++]=
        }
    }
    fprintf(g,"%d",nr);
    fclose(f);
    fclose(g);
    return 0;
}
