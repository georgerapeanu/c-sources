#include <fstream>
using namespace std;
ifstream f("invector.in");
ofstream f("invector.out");
int n,v[100000],i;
int main()
{
    f=fopen("invector.in","r");
    fscanf(f,"%u",&n);
    for(i=1;i<=n;i++)
    fscanf(f,"%u",&v[i]);
    fclose(f);
    for(i=n;i>=1;i--)
    fprintf(g,"%u ",v[i]);
    fclose(g);
    return 0;
}
