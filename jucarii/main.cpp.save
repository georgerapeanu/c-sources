#include <iostream>

using namespace std;
int pas,V[20];
bool used[20];
bool ok()
{
    int best[20];
    for(int i=0;i<n;i++)
    {
        best[i]=0;
        for(int j=0;j<i;j++)
        {
            if(V[j]>V[i])
            {
                best[i]=max(best[j]+1,best[i]);
            }
        }
        if(best[i]>=K)
            return 1;
    }
    return 0;
}
int back(int s,int p,int n)
{
    if(s>n)
        {if(ok())pas++;}
    else
    {
        if(pas<p)
        {
            for(int i=1;i<=n;i++)
            {
                if(!used[i])
                {
                    used[i]=1;
                    V[s]=i;
                    back(s+1,p,n);
                    used[i]=0;
                }
            }
        }
    }
}
int main()
{
    fscanf()
    return 0;
}
