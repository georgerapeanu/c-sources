#include <cstdio>
using namespace std;
FILE *f=fopen("custi.in","r");
FILE *f=fopen("custi.out","w");
int dp[1001][1001];//dp[i][j]=min(dp[i-1][j],dp[i][j-1]+1
int frecv[1000001];
int i,j,N;
int main()
{
    fscanf(f,"%d",&N);
    for(i=1;i<=N;i++)
    {
        for(j=1;j<=N;j++)
        {
            fscanf(f,"%d",&dp[i][j]);
            if(dp[i][j])
                dp[i][j]=min(dp[i-1][j],dp[i][j-1])+1;
            frecv
        }
    }
    return 0;
}
