#include <iostream>
using namespace std;
int N;
long long V[400005];
int poz[400005];
int main()
{
    cin>>N;
    poz[0]=0;
    for(i=1;i<=N;i++)
    {
        cin>>V[i];
        V[i]=(V[i-1]+V[i])%N;
        if(poz[V[i]])
            break;
        poz[V[i]]=i;
    }
    for(j=)
    return 0;
}
