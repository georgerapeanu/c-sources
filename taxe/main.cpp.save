#include <fstream>
using namespace std;
ifstream f("taxe.in");
ofstream g("taxe.out");
long long S,i,st=1,dr,mid;
long long ok(int nr)
{
    
}
int main()
{
    f>>S;
    dr=S/4;
    S/=4;
    last=-1;
    while(st<=dr)
    {
        mid=(st+dr)/2;
        if(ok(mid)<=S)
        {
            last=mid;
            st=last+1;
        }
        else
            dr=last-1;
    }
    g<<last;
    f.close();
    g.close();
    return 0;
}
