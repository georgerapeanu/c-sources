#include <cstdio>
#include <algorithm>
using namespace std;
FILE *f=fopen("caluti.in","r");
FILE *g=fopen("caluti.out","w");
int best[100005],K,N,T,X,P,j,i;
int main()
{
    fscanf(f,"%d",&T);
    for(i=1;i<=T;i++)
    {
        fscanf(f,"%d %d %d",&N,&K,&P);
        for(j=1;j<=N;j++)
        {
            fscanf(f,"%d",&X);
//            best[j]=max(best[max(0,j-K)],X)+P;
            
        }
        fprintf(g,"%d\n",best[N]);
    }
    fclose(f);
    fclose(g);
    return 0;
}
