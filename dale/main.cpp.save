#include <cstdio>
#include <vector>
using namespace std;
FILE *f=fopen("dale.in","r");
FILE *g=fopen("dale.out","w");
int n,i,j,l;
vector <short> V;
int main()
{
    V.push_back(0);
    V.push_back(1);
    l=1;
    for(i=1;i<=n+1;i++)
    {
        for(j=l;j>=1;j--)
            V[j]*=2;
        for(j=l;j>=2;j++)
        {
                V[j-1]+=V[j]/10;
                V[j]%=10;
        }
        if(V[1]>9)
        {
            l++;
            V[1]
            V.insert(V.begin(),V[1]/10);
            V[2]%=10;
        }
    }
    for(i=1;i<=l;i++)
    return 0;
}
