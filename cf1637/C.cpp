#include <bits/stdc++.h>

using namespace std;

int main(){
  
  int t;

  scanf("%d", &t);

  while(t--){
    int n;
    scanf("%d", &n);
    vector<int> v(n, 0);

    for(auto &it:v){
      scanf("%d", &it);
    }

    vector<int> odd_stack;
    int nxt_odd = 1;
    int ans = 0;

  }

    return 0;
}
