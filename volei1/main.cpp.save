#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;
FILE *f=fopen("volei1.in","r");
FILE *g=fopen("volei1.out","g");
typedef pair<int,int> punct;
int F[2010],N,B[2010],M,st,nr,K,j;
punct C[2010];
vector <punct> V;
int main()
{
    fscanf(f,"%d",&N);
    for(i=1;i<=N;i++)
    {
        fscanf(f,"%d",&F[i]);
        if(i-1)
            C[i]=punct(max(F[i-1],F[i]),i);
        
    }
    C[i]=punct(max(F[1],F[N]),i);
    fscanf(f,"%d",&M);
    for(i=1;i<=M;i++)
        fscanf(f,"%d",&B[i]);
    sort(C+1,C+1+N);
    sort(B+1,B+1+N);
    i=1;
    while(K<=N&&i<=M)
    {
        if(C[K].first<B[i])
        {
            K++;
            V.push_back(C[K].second,B[i]);
        }
        i++;
    }
    sort(V.begin(),V.end()-1);
    fprintf(g,"%d\n",K);
    for(i=1;i<=N;i++)
    {
        fprintf(g,"%d ",F[i]);
        if(V[j].first)
    }
    return 0;
}
