#include <cstdio>
#include <bitset>
using namespace std;
FILE *f=fopen("submat.in","r");
FILE *g=fopen("submat.out","w");
short N,M,i,j;
int maxim;
short nr;
int V[1005];
short val;
int main()
{
    fscanf(f,"%d %d",&N,&M);
    for(i=1;i<=N;i++)
    {
        for(j=1;j<=M;j++)
        {
            fscanf(f,"%d",&B[i][j]);
        }
    }
    qs(1,N);
    for(i=1;i<=N;i++)
    {
        j=1;
        while(B[i][j]==0&&j<=M)
            j++;
        j--;
        if(j*i>maxim)
            maxim=j*i;
    }
    fprintf(g,"%d",maxim);
    fclose(f);
    fclose(g);
    return 0;
}
