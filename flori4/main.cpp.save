#include <cstdio>

using namespace std;
int x=2,y=3,tmp,i,N;
int main()
{
    fscanf(f,"%d",&N);
    if(N==1)
        fprintf("1");
    else if(N==2)
        fprintf("2");
    else
    {
        i=2;
        while(i<N)
        {
            tmp=y;
            y=(x+y)%9001;
            x=tmp;
            i++;
        }
        y--;
        if(y<0)
            y=9000;
        y%=9001;
        fprintf(g,"%d",y);
    }
    fclose(f);
    fclose(g);
    return 0;
}
