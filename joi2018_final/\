#include <bits/stdc++.h>

using namespace std;

int main(){
  
  int n;
  vector<int> v(n, 0), dp(n, 0);

  for(auto &it:v){
    scanf("%d %d", &it.first, &it.second);
  }

  for(int i = 0;i < n;i++){
    dp[i] = max(b[i], b[i] - (a[i] - a[i - 1]))
  }

  return 0;
}

