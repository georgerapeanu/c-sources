#include <cstdio>
#include <cstring>
using namespace std;
char C[105];
char rez[105]
int N,i,j,cnt;
int main()
{
    cin.getline(C,105);
    N=strlen(C);
    for(i=0;i<N;i++)
    {
        rez[cnt]=C[i];
        while(rez[cnt]==rez[cnt-1])
            cnt-=2
    }
    return 0;
}
