#include <cstdio>
using namespace std;
FILE *f=fopen("zar.in","r");
FILE *g=fopen("zar.out","w");
int N,M,nr,tip,poz,i,j,L,C;
int indicatoare[60][60];
int main()
{
    fscanf(f,"%d %d",&N,&M);
    for(i=1;i<=N;i++)
    {
        for(j=1;j<=M;j++)
        {
            fscanf(f,"%d %d %d",&nr,&tip,&poz)
        }
    }
    return 0;
}
