#include <cstdio>
using namespace std;
FILE *f=fopen("meteo1.in","r");
FILE *g=fopen("meteo1.out","w");
int n,i,V[1010],maxim,lg,x,y,bestx;
int main()
{
    lg=1;
    maxim=1;
    x=y=1;
    fscanf(f,"%d",n);
    for(i=1;i<=n;i++)
    {
        fscanf(f,"%d",&V[i]);
    }
    for(i=2;i<=n;i++)
    {
        if((V[i-1]<0&&V[i]>=0)||(V[i-1]>=0&&V[i]<0))
        {
            lg++;
        }
        else
        {
            if(lg>maxim)
            {
                maxim=lg;
                bestx=x;
                y=lg;
                x=i;
            }
        }
    }
    for(i=bestx;i<=x+lg-1;i++)
    {
        fprintf(g,"%d ",V[i]);
    }
    fclose(f);
    fclose(g);
    return 0;
}
