#include <cstdio>
#include <iostream>
#include <cstring>
#define val first
#define sens second
using namespace std;
FILE *f=fopen("palalila2.in","r");
FILE *g=fopen("palalila2.out","w");
int N,i,j,rez;
char C[600000];
int main()
{
    C[0]='.';
    fgets(C+1,600000,f);
    N=strlen(C)-1;
    i=1;
    while(i<=N)
    {
        while(C[i]>=C[i+1]&&i+1<=N)
        {
            
        }
    }
    fprintf(g,"%d",rez);
    return 0;
}
