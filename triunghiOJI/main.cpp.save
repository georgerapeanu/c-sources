#include <cstdio>
#include <iostream>
using namespace std;
FILE
long long rand[2][1005];
long long  N,i;
pair<long long,long long> data[1005];
int main()
{
    fscanf(f,"%l64d",&N);
    for(i=1;i<=N;i++)
        fscanf(f,"%l64d",&data[i].first,&data[i].second);
    rand[N%2][1]=data[N].second;
    for(i=N-1;i>=1;i--)
    {
        rand[i%2][data[i].first]=data[i].second;
        for(j=data[i].second+1;j<=N-i+1;j++)
            rand[i%2][j]=rand[(i+1)%2][j-1]-rand[i%2][j-1];
        for(j=data[i].second-1;j>0;j--)
            rand[i%2][j]=rand[(i+1)%2][j]-rand[i%2][j+1];
    }
    for(i=1;i<=N;i++)
        fprintf(g,"%l64d ",rand[1][i]);
    fclose(f);
    fclose(g);
    return 0;
}
